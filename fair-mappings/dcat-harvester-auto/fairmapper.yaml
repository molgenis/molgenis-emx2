name: dcat-harvester-auto
version: 1.0.0

mappings:
  - name: harvest-catalog
    fetch: ${SOURCE_URL}
    frame: src/frames/resources-auto.jsonld
    steps:
      - transform: src/transforms/to-molgenis-simple.jslt
      - mutate: src/mutations/upsert-resources.gql
