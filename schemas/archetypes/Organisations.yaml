name: Organisations
columns:
  Resources/*: ~
  #get all columns from resources
  Organisation type:
    type: codelist
    allowOther: true
    # so then the code is rendered with additional 'other' freetext field
    refTable: Organisation Types
  institution:
    type: text
  address:
    type: text
  features:
    type: taxonomylist
    refTable: Organisation features
  leading resources:
    type: refback
    refTable: Resources
    refBack: lead organisation

