table: Collections
description: Collections of data, such as cohort studies, biobanks and registries
subsets: [aggregate_core]
columns:
  id:
    key: 1
    required: true
    description: IDs taken from the catalogue table 'Collections'
  display name:
    description: Collection name for display in aggregate dashboard
