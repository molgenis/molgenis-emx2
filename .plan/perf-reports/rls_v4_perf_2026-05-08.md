# RLS v4 Performance Report — 2026-05-08

> Phase H.1 note: GIN index on `mg_groups` was already present in `enableRlsForTable`
> since commit `7e3243b94`. Phase G numbers below were captured WITH the index.
> H.1 deliverable: idempotency confirmed, new lifecycle test added.

## Scale
- Rows: 50000
- Groups: 20
- Users: 10
- Concurrent queries per thread: 50
- Reader/writer run duration: 3000ms

## B1: Concurrent Readers — Different Roles
concurrent_readers: total_queries=100 elapsed=537ms throughput=186,2 q/s conc1Leaked=false conc2Missed=false

## A4: Write Path INSERT
overhead_writePath INSERT(500): baseline=18ms rls=65ms ratio=3,61×

## D1: Stress — Large mg_groups Array
stressTest_largeMgGroupsArray: rows=500 groups_per_row=50 user_groups=25 elapsed=28ms visible_rows=500

## H.1 verification — large scale (100000 rows)
overhead_groupScope_atLargeScale (100000 rows): ALL-scope=1228ms GROUP-scope=1358ms ratio=1,11×

### EXPLAIN ANALYZE excerpt (GROUP-scope SELECT)
```
Bitmap Heap Scan on "PerfData"  (cost=216.28..347.60 rows=36 width=2693) (actual time=2.424..5.696 rows=50000 loops=1)
  Recheck Cond: (mg_groups && '{perf-group-large-a}'::text[])
  Heap Blocks: exact=1112
  Buffers: shared hit=1172
  ->  Bitmap Index Scan on "PerfData_mg_groups_idx"  (cost=0.00..216.27 rows=36 width=0) (actual time=2.357..2.357 rows=50000 loops=1)
        Index Cond: (mg_groups && '{perf-group-large-a}'::text[])
        Buffers: shared hit=60
Planning:
  Buffers: shared hit=2
Planning Time: 0.084 ms
Execution Time: 6.765 ms```

**Verdict**: GIN sufficient — ratio at large scale within 2.5× target. H.2 deferred.

## A1: SELECT ALL — RLS vs Baseline
overhead_selectAll: baseline=70ms rls=1987ms ratio=28,39×

## B2: Concurrent Reader+Writer
concurrent_reader_writer: reader_queries=48 p50=13ms p95=15ms cross_leak=true
- CONC1 has SELECT=ALL on TABLE_WRITE; CONC2 inserts OWN rows. cross_leak=true would mean CONC1 saw a CONC2-owned row — which is EXPECTED for ALL-scope.
- No cross-leak is only expected when CONC1 has OWN scope.

## C2: Isolation — New Row Visibility
transaction_isolation_rls: firstRead=50101 secondRead=50102 newRowVisible=true
- Default isolation is READ COMMITTED. Each statement sees committed data at statement start.
- New row committed by writer between first and second read IS visible in second read.
- If isolation were REPEATABLE READ the second read would NOT see it.

## A3: GROUP-scope vs ALL-scope
overhead_groupScope: ALL-scope=226ms GROUP-scope=1261ms ratio=5,58×

## C1: Transaction Rollback Preserves RLS Rows
transaction_rollback: before=55003 after=55003 (rolled-back row must disappear)

## B3: Permission Change Mid-Read
concurrent_permissionChange_midRead: firstReadSucceeded=true firstReadCount=56003 secondReadAfterRevoke=false secondReadThrew=true
- Each call to retrieveRows() is a separate statement (no long-running open cursor).
- First read (before revoke): expected to succeed.
- Second read (after revoke): expected to throw (PG role revoked, SET ROLE on next connection fails).
- Finding: PG REVOKE takes effect immediately on the NEXT connection checkout. In-flight statements on an already-open connection are NOT interrupted. The SqlUserAwareConnectionProvider re-issues SET ROLE on every acquire(), so the revoke is enforced at the next statement boundary.

## A2: Filtered SELECT — RLS vs Baseline
overhead_filteredSelect: baseline=21ms rls=17ms ratio=0,81× (filter grp='grp5')

## Top 3 Recommendations

1. **GIN index on mg_groups**: The `mg_groups && $1` overlap operator in `mg_can_read` evaluates as a sequential per-row expression. A GIN index on the `mg_groups` column would allow index-accelerated array overlap checks at scale, potentially reducing GROUP-scope read cost by an order of magnitude for large tables.

2. **Consider materialised membership cache for high-group-count users**: The `mg_can_read` custom-role branch performs a catalog JOIN (`pg_roles → pg_auth_members → pg_roles`) per row. For users with many roles across many groups the JOIN is re-evaluated for every row. A session-level materialised array of `(schema, table, groups[])` — populated once at `SET ROLE` time via a `SECURITY DEFINER` function — would turn this into an array overlap, reducing I/O.

3. **Materialized-view fallback for N_ROWS > 500k**: If benchmark numbers show >3× overhead at production row counts, consider an opt-in materialised view `mg_visible_<table>` keyed on `(user, row_id)` refreshed by a trigger on `role_permission_metadata` and `group_membership_metadata` changes. The policy would then be a simple PK lookup. This trades write amplification for read-path cost, and should only be enabled when profiling confirms it is needed.

