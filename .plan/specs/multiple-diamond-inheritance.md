# Spec: Unified Discriminator-Driven Composition (Diamond Inheritance + Modules)

Living guardrail. `Test` links the validating test once written; `Visual` marks manual checks.
Status: Phase A + Phase B (diamond inheritance, EXCLUSIVE) DONE & green. Phase C REVISED (2026-06-12) =
unified column-based discriminator engine, staged C0–C6 (absorbs old Phase D MODULE + Phase G multi-axis).
C0 (discriminator abstraction + rename) DONE & green (2026-06-12, reviewed; trimmed to Design B — scalar
`SUBCLASS` type DROPPED, `mg_tableclass` stays master-style untyped, discriminator engine = `MODULE_ARRAY` only).
C1 (MODULE_ARRAY DDL + value validation) DONE & green (2026-06-12, reviewed PASS-WITH-FIXES, all addressed):
MODULE_ARRAY persists `varchar[]` + survives reload; `values` validated to reference existing `tableType=MODULE`
tables (non-existent/DATA/ONTOLOGY/malformed rejected); enum-family membership enforced on insert+update via core
`TypeUtils.checkEnumMembership` (only when `values` non-empty); mg_tableclass (is-a) + MODULE_ARRAY (composition)
coexist (mg_tableclass readonly/immutable, MODULE_ARRAY mutable, no trigger/name collision). NO materialization/
write-routing/query/gating/hard-delete yet (C2–C6). C2–C6 PLANNED.

**MODEL B PIVOT (2026-06-12, owner):** a MODULE is a **real, queryable table bound to ONE root** (NOT
materialized per consumer). One storage model = Phase B table-per-type on shared root PK; two discriminator
flavors: is-a/`mg_tableclass` (EXCLUSIVE, immutable) vs composition/`MODULE_ARRAY` (MULTIPLE, mutable, no
mg_tableclass). C2 = flag split (skipPk+skipMgTableclass), MODULE-extends-root → real subtype table w/o
mg_tableclass, exclude MODULE subtypes from is-a enumeration, tighten MODULE_ARRAY value validation to
"extends this root" + one-axis, reload round-trip. Materialization/reserved-naming DROPPED; cross-root reuse
intentionally LOST (reversible later). Binding model LOCKED = explicit: module `extends root` +
`tableType=MODULE` (storage binding) AND root's `MODULE_ARRAY.values` list the module per axis (axis
assignment); engine cross-checks each value extends this root + one-axis-per-module (O-5).

**C2 DONE & green (2026-06-13, independently re-verified; STAGED, not committed).** C2.A–C2.E landed + the
`executeSetInherit` param-elimination simplification (single derived 3-arg form). MODULE-extends-root = real subtype
table (PK+FK+KEY1, no mg_tableclass); MODULE excluded from is-a enumeration (catalog-visible); MODULE_ARRAY value must
extend this root + one-axis (O-5); reload round-trip. Verified exit 0 across diamond/module/cross-schema/reload suites;
spotless+PMD clean.

**C3 DONE & green (2026-06-13, independently reviewed NO-BLOCKERS; STAGED, not committed).** MULTIPLE write routing: insert/update
of a row on root R writes a row into each ACTIVE module subtype table (named by the MODULE_ARRAY value(s)) on the shared root PK, in
addition to the root row — generalizes the is-a chain write (is-a path untouched; empty-discriminator = exact no-op). Validation is
one-go over `base ∪ active-module columns` before the chain; update = upsert active modules; module-extends-module writes the full
module-ancestor chain in FK order. NO query projection/gating/hard-delete yet — C4–C6 PLANNED; C4 GENERALIZES
`tableWithInheritanceJoin` into ONE discriminator-driven join (not a parallel composition method); C6 hard-deletes removed modules.

**C4 DONE & green (2026-06-13; independently reviewed NO-BLOCKERS, fixes applied, full-sql/core regression green; STAGED, not committed).** MULTIPLE query
projection: a module's columns are READABLE BY NAME when querying root R. Gating is **ROW PRESENCE ONLY** (LEFT JOIN) —
**NO CASE-WHEN / NO `= ANY` predicate** (owner 2026-06-13): the unified join LEFT-JOINs module subtype tables on the shared
root PK exactly like is-a subclasses; a module col is non-null iff its (C3-written) row exists. Changes (3 files): new
`TableMetadata.getModuleSubtypeTables()`/`collectModuleSubtablesDeduped()` (transitive MODULE subtypes, mirror of is-a pair,
filter `isModule()`), `getColumnsIncludingActiveModules()` + `getColumnByNameIncludingActiveModules()`; `SqlQuery.tableWithInheritanceJoin`
appends a deduped module LEFT-JOIN loop after the (UNTOUCHED) is-a loop; `SqlQuery.getColumnByName` broadened to resolve module
cols (additive only). `rowSelectFields` UNCHANGED. is-a path byte-identical (TestDiamondInheritance 13 + TestInherits 2 green
UNMODIFIED). The predicate-bearing `DiscriminatorAxis` was NOT introduced (premise void under row-presence gating). DEFERRED:
implicit/default-select inclusion + GraphQL field surface (Phase E), filter/order BY a module col, hard-delete (C6).

| Behavior | Component | Test | Visual |
|----------|-----------|------|--------|
| A table may declare multiple parents via `setInheritNames(String...)` (no `addInheritName`) | TableMetadata / SqlTableMetadata | TestTableMetadataDag, TestDiamondInheritance | - |
| Multi-parent DAG validated ONCE before any DDL — rejection leaves no partial state | SqlTableMetadata.setInheritNames | TestDiamondInheritance.twoRootsRejected | - |
| Inheritance DAG must have exactly one common root; >1 root throws | TableMetadata.getRootTable | TestDiamondInheritance.twoRootsRejected | - |
| Cyclic inheritance is rejected | TableMetadata DAG traversal | TestTableMetadataDag.getAllInheritNamesThrowsOnGenuineCycle | - |
| Columns reachable via two parents appear once, stable position | TableMetadata.getColumns/getLocalColumns | TestTableMetadataDag.getColumnsIncludingSubclassesDeduplicatesInDiamond, getSubclassTablesDeduplicatesInDiamond | - |
| Duplicate column name across parents/modules = validation error | SqlTableMetadata.validateInheritanceDag | TestDiamondInheritance.duplicateColumnAcrossParentsRejected, addColumnRejectedWhenCollidesViaSecondParentSubclass | - |
| Existing single `extends` schemas load & behave unchanged (desugar) | TableMetadata + SqlTableMetadata | TestInherits, TestExtends, TestCrossSchemaForeignKeysAndInheritance (green) | - |
| Generic `ENUM` / `ENUM_ARRAY` column types exist (string + `values` list) | ColumnType | TestTableMetadataDag | - |
| Column type `MODULE_ARRAY` (composition; renamed from `SUBCLASS_ARRAY` 2026-06-12) exists (enum family, NOT ref). Scalar `SUBCLASS` type DROPPED (Design B) — is-a uses untyped `mg_tableclass` | ColumnType | TestTableMetadataDag | - |
| `Column.values` (List<String>) holds the allowed choices; for is-a/`mg_tableclass` these are `schema.Table` names; for `MODULE_ARRAY` they are BARE table names in the declaring schema; all validated to exist | Column / MetadataUtils | MetadataPersistenceRoundtripTest (persistence); **existence-validation DONE C1** — TestModuleArrayDiscriminator.moduleArrayValuesRejectNonExistentTable / RejectDataTable / RejectOntologyTable / moduleArrayValuesRejectAnyDottedValueAcceptBareName | - |
| Composition discriminator is a first-class `MODULE_ARRAY` column; engine loops `getDiscriminatorColumns()` (= MODULE_ARRAY cols). is-a (`mg_tableclass`) is separate, NOT in this set | TableMetadata.getDiscriminatorColumns / Column.isDiscriminator | TestTableMetadataDag.getDiscriminatorColumnsFindsModuleArrayColumns | - |
| Discriminator chosen value(s) come from `Column.values`: is-a/`mg_tableclass` = `schema.Table` (cross-schema OK); `MODULE_ARRAY` = BARE table name (same schema only) | SqlTable / discriminator | TestModuleArrayDiscriminator (C3 write routing) | - |
| built-in `mg_tableclass` stays the master-style untyped, engine-managed, immutable system column (Design B: NOT typed `SUBCLASS`; executor unchanged from Phase B) | SqlTableMetadataExecutor | _C0 DONE — TestDiamondInheritance / TestInherits / MetadataPersistenceRoundtripTest green_ | - |
| `Column.values` applies to `MODULE_ARRAY` columns (the allowed MODULE set as BARE table names in the same schema, validated to exist); `mg_tableclass`/is-a needs no `values` | Column / MetadataUtils / SqlColumnExecutor.validateModuleArrayValues | **C1 DONE** — TestModuleArrayDiscriminator.moduleArrayColumnPersistsAndSurvivesReload + RejectNonExistentTable/RejectDataTable/RejectOntologyTable | - |
| Enum-family values constrained to `Column.values` (membership) on INSERT and UPDATE, only when `values` non-empty; logic in core `TypeUtils.checkEnumMembership`, invoked from SQL write path | TypeUtils.checkEnumMembership / SqlTypeUtils.checkValidation | **C1 DONE** — TestTypeUtils.checkEnumMembership* (5 unit) + TestModuleArrayDiscriminator.insertRejectsOutOfSetValueAndAcceptsInSetValue / updateRejectsOutOfSetValue / enumScalarWithValuesEnforcesAllowedSet / enumWithNoValuesFreeStringAccepted | - |
| `MODULE_ARRAY` values are BARE table names only, resolved in the declaring table's OWN schema; ANY dotted/`schema.Table` value is REJECTED (modules are same-schema only — cross-schema dropped, owner 2026-06-13); membership is plain EXACT-string match, uniform with ENUM/ENUM_ARRAY (the earlier `moduleRefIdentity`/identity machinery was DELETED). is-a/`mg_tableclass` unaffected (stays `schema.Table`, cross-schema) | SqlColumnExecutor.validateModuleArrayValues / validateNoModuleInMultipleAxes / TypeUtils.checkEnumMembership | **DONE (2026-06-13, owner-requested bare-only simplification)** — TestModuleArrayDiscriminator.moduleArrayAcceptsBareValueDefaultingToCurrentSchema / insertWithBareModuleValueActivatesModule / moduleArrayRejectsSchemaQualifiedValue / moduleArrayValuesRejectAnyDottedValueAcceptBareName + TestTypeUtils.enumExactMatchEnforced | - |
| is-a identity (`extends` → `mg_tableclass`, single/EXCLUSIVE) references TABLE/DATA subclasses incl. cross-schema | SqlTable / SqlQuery | TestDiamondInheritance; cross-schema = TestCrossSchemaForeignKeysAndInheritance | - |
| `MODULE_ARRAY` (multi, MULTIPLE) references MODULE classes ALWAYS; stored `varchar[]`; COEXISTS with scalar `mg_tableclass` (does NOT replace it) | SqlTable / SqlQuery / SqlTableMetadataExecutor | **DDL+storage DONE C1** — TestModuleArrayDiscriminator.moduleArrayColumnPersistsAndSurvivesReload, mgTableclassAndModuleArrayCoexist; _write/query routing C3–C4_ | - |
| Inheritance (`extends`/`mg_tableclass`) and composition (`MODULE_ARRAY`) may coexist on one table | SqlTable / SqlQuery / SqlTableMetadataExecutor | **DDL/metadata coexistence DONE C1** — TestModuleArrayDiscriminator.mgTableclassAndModuleArrayCoexist (mg_tableclass present + readonly; 2 MODULE_ARRAY independently validated; no collision); _write/query mix smoke C6_ | - |
| Multiple `MODULE_ARRAY` columns = independent modular choice-sets (orthogonal axes) | TableMetadata.getDiscriminatorColumns | **C0 accessor + C1 independent validation** — TestModuleArrayDiscriminator.mgTableclassAndModuleArrayCoexist (2 discriminators); _runtime C3–C6_ | - |
| Diamond child has one PK, one FK per parent, all on single root PK | SqlTableMetadataExecutor.executeSetInherit | TestDiamondInheritance.diamondChildHasSingleRootPrimaryKey, diamondChildHasForeignKeyPerParent, mgTableclassLivesOnlyOnRoot | - |
| Insert into diamond writes shared root exactly once | SqlTable.insertBatch | TestDiamondInheritance.insertAndSelectRoundtripThroughDiamond | - |
| Query joins distinct ancestor set on single root PK | SqlQuery.tableWithInheritanceJoin | TestDiamondInheritance.insertAndSelectRoundtripThroughDiamond, diamondSurvivesSchemaReload | - |
| Insert of a row activating N modules writes a row into each active module subtype table on the shared root PK (generalized chain loop + per-table row-batch filter; is-a path unchanged) | SqlTable.insertBatch / insertModuleRows | **C3 DONE** — TestModuleArrayDiscriminator.insertActivatingTwoModulesWritesRowInEachModuleTable | - |
| Activating one module writes only that module's row; other module tables have no row for that PK | SqlTable.insertBatch / insertModuleRows | **C3 DONE** — TestModuleArrayDiscriminator.insertActivatingOneModuleWritesOnlyThatModuleRow | - |
| Module-extends-module: activating a deep module writes its full module-ancestor chain in FK order on the shared PK | SqlTable.expandModuleAncestors / insertModuleRows | **C3 DONE** — TestModuleArrayDiscriminator.moduleExtendsModuleWritesFullModuleChainInFkOrder | - |
| Active module columns validated/computed in ONE pass over base ∪ active-module columns before the chain (inactive never validated) | SqlTable.applyValidationPerRow + SqlTypeUtils.applyValidationAndComputed | **C3 DONE** — covered by insertActivatingOneModuleWritesOnlyThatModuleRow (no error on inactive module col) | - |
| Update upserts active module rows (creates on newly-activated, updates already-active); removed-module hard-delete DEFERRED to C6 | SqlTable.updateBatch / insertModuleRows | **C3 DONE** — TestModuleArrayDiscriminator.updateUpsertsNewlyActivatedModuleRow (removal=C6) | - |
| Module columns of R selectable BY NAME + projected via the unified join; active⇔present is gated by ROW PRESENCE only (LEFT JOIN), NOT a CASE-WHEN — write side (C3 upsert / C6 delete) keeps row⇔active (owner 2026-06-13). rowSelectFields UNCHANGED; changes = tableWithInheritanceJoin LEFT-JOINs module subtypes + getColumnByName resolves module cols | SqlQuery.tableWithInheritanceJoin / getColumnByName + TableMetadata module-subtype surface | **C4 DONE** — TestModuleArrayDiscriminator.queryRootProjectsActiveModuleColumns / queryRootNullsInactiveModuleColumn / queryRootProjectsModuleExtendsModuleChain / queryMixedBatchProjectsPerRowModuleColumns | - |
| required/visible/computed of module columns gated by the active set — ACTIVE module's columns validated/computed/required; INACTIVE excluded (never required). **SUBSUMED BY C3** (`SqlTable.buildValidationUnion` = base ∪ active-module columns → `SqlTypeUtils.applyValidationAndComputed`); discriminator columns are in the JS graph so `visible`/`required` expressions can reference activation directly. `__active` engine binding NOT built (ergonomic, deferred); visible-FLAG surfacing for forms = Phase F | SqlTable.applyValidationPerRow/buildValidationUnion + SqlTypeUtils.applyValidationAndComputed | **C5 DONE (subsumed by C3, locked 2026-06-13)** — TestModuleArrayDiscriminator.requiredModuleColumnEnforcedOnlyWhenModuleActive (active+omit→throws / active+provide→ok / inactive+omit→ok) | - |
| Deactivating a module on update HARD-DELETEs its materialized row (root row intact); removed = oldActive \ effectiveNewActive (array-omission nets to zero); explicit child-first `deleteBatch` per removed module; partial-update NULLing left as-is (owner 2026-06-13, consistent w/ is-a) | SqlTable.updateBatch | **C6 DONE & green, reviewed NO-BLOCKERS (2026-06-13; STAGED, not committed)** — TestModuleArrayDiscriminator.updateRemovesDeactivatedModuleRow / deactivatedModuleColumnProjectsNullAfterDelete / moduleExtendsModuleDeactivationRemovesFullChainInOrder / moduleExtendsModuleDeactivationKeepsSharedAncestorModule (diff granularity) / mixIsAAndModuleArrayDeactivationLeavesIsAIdentityIntact (asserts mg_tableclass survives) | - |
| Discriminator named column: `mg_tableclass` is the reserved auto-name from `extends`; `MODULE_ARRAY` columns are modeler-named | SqlTableMetadataExecutor | TestDiamondInheritance.mgTableclassLivesOnlyOnRoot, mgTableclassOnRootOnlyInSingleChain | - |
| `tableType=MODULE` (MODEL B) = a real, queryable composition-subtype table bound to ONE root; extends the root with NO mg_tableclass | TableType / SqlTableMetadataExecutor | **C2 DONE** — TestModuleArrayDiscriminator.moduleExtendsRootIsRealTableWithPkFkKey1NoMgTableclass | - |
| MODULE-extends-root → real subtype table keyed on root PK (PK+FK+KEY1), NO mg_tableclass; via param-free derived executeSetInherit (primary-parent ⇒ PK/KEY1; tableType==MODULE ⇒ skip mg_tableclass) | SqlTableMetadataExecutor.executeSetInherit | **C2 DONE** — TestModuleArrayDiscriminator.moduleExtendsRootIsRealTableWithPkFkKey1NoMgTableclass; diamond unchanged: TestDiamondInheritance (13) | - |
| MODULE composition subtypes excluded from is-a enumeration (getSubclassTables / is-a query joins) but stay catalog-visible real tables | TableMetadata.collectSubclassTablesDeduped / SqlQuery | **C2 DONE** — TestModuleArrayDiscriminator.moduleSubtypesExcludedFromIsAEnumeration | - |
| A module may extend other modules (columns flatten via normal inheritance, all rooted at the one root — NO copy/materialize) | TableMetadata.getColumns | **C2 DONE** (transitive root-match) — TestModuleArrayDiscriminator.moduleArrayValueMustExtendThisRoot | - |
| MODULE_ARRAY value must reference a MODULE that EXTENDS this root (shares root) + at most one axis per module (O-5) | SqlColumnExecutor.validateModuleArrayValues / validateNoModuleInMultipleAxes | **C2 DONE** — TestModuleArrayDiscriminator.moduleArrayValueMustExtendThisRoot / moduleArrayValueOneAxisPerModule | - |
| A MODULE_ARRAY column must be declared on the ROOT of its hierarchy (declaring table == its own root); declaring it on a non-root subtype is REJECTED at validation (was a latent runtime SQL error in the C6 prior-array read; canonical Model B = root declares axes; reversible to per-owning-table prior-read if subtype axes ever needed) | SqlColumnExecutor.validateModuleArrayValues | **C6 review S1 DONE** — TestModuleArrayDiscriminator.moduleArrayColumnMustBeDeclaredOnRoot | - |
| Module subtype tables + MODULE_ARRAY survive schema reload (no new metadata column / migration) | MetadataUtils (existing persistence) | **C2 DONE** — TestModuleArrayDiscriminator.moduleSubtypesAndModuleArraySurviveReload | - |
| Binding is EXPLICIT: module declares `extends root` + tableType=MODULE (storage) AND root's MODULE_ARRAY.values list it (axis); a column edit never mutates the module table (no adoption side-effect) | SqlColumnExecutor / SqlTableMetadataExecutor | **C2 DONE** — TestModuleArrayDiscriminator.moduleArrayValueMustExtendThisRoot | - |
| `MODULE_ARRAY` values are MODULE classes only; `extends`/`mg_tableclass` values are TABLE subclasses | SqlColumnExecutor.validateModuleArrayValues / validation | **C1 DONE (array)** — TestModuleArrayDiscriminator.moduleArrayValuesRejectDataTable / RejectOntologyTable / RejectNonExistentTable; is-a = done | - |
| Discriminator allowed set lives in one `values` field (replaces `refTable`-base + `allowedSubclasses[]`) | Column / MetadataUtils | MetadataPersistenceRoundtripTest | - |
| DB migration: `table_inherits`→`VARCHAR[]` + `column_metadata."values"`; old DB upgrades | Migrations | migration32 (DB v33) + MetadataPersistenceRoundtripTest (TestMigration extension = TODO, deferred per nonparallel-module guidance) | - |
| EMX2 CSV roundtrips multi-parent, MODULE, ENUM/MODULE_ARRAY, `values` | Emx2 IO | **E0 DONE** — Emx2ModuleRoundtripTest.moduleTableTypeAndColumnValuesRoundtrip (MODULE tableType + multi-parent extends + MODULE_ARRAY/ENUM + `values` all round-trip) | - |
| Module columns of root R flow IMPLICITLY via widened `retrieveRows` default-select (`getColumnsIncludingModules()` = getColumns ∪ module cols, NOT subclasses → is-a byte-identical); non-module tables = no-op. RDF (retrieveRows) benefits; GraphQL/JSON path = E2; CSV-export header = E1b | SqlQuery retrieveRows default-select / TableMetadata.getColumnsIncludingModules | **E1 DONE** — TestModuleArrayDiscriminator.defaultSelectIncludesActiveModuleColumn / defaultSelectNullsInactiveModuleColumn; is-a byte-identical (TestDiamondInheritance 13 + TestInherits 2 green UNMODIFIED) | - |
| CSV download/export includes module columns + import recognizes them; full export→re-import round-trip recreates module rows (C3 routing on save) | TableMetadata.getDownloadColumnNames / Emx2Tables.isFileType | **E1b DONE** — TestModuleCsvDownload.csvExportIncludesModuleColumnAndValue / csvExportModuleColumnNullForInactiveRow / csvExportReimportRoundTrip | - |
| Module columns are SELECTABLE in the GraphQL data query (output type-builder widened; JSON projection resolves module cols; MODULE_ARRAY itself renders via ENUM_ARRAY arm; filter/order-BY a module col still deferred) | GraphqlTableFieldFactory / SqlQuery jsonSubselectFields | **E2 DONE** — TestModuleGraphqlDataQuery.* + TestModuleArrayDiscriminator.retrieveJsonSelectsActiveModuleColumn / retrieveJsonNullsInactiveModuleColumn | - |
| Diamond schema survives `SqlSchema.merge` and JSON-model round-trip with ALL parents preserved (not just primary) | SqlSchema.merge, json/Table | TestDiamondInheritance.mergePreservesAllParentsOfDiamondChild, TableJsonRoundtripTest.jsonRoundtripPreservesAllParentsOfDiamondChild | - |
| GraphQL `_schema` + JSON expose inheritNames + `values` (back-compat inheritName kept); table-level `modules` field SKIPPED (derivable from MODULE tableType + MODULE_ARRAY `values`) | GraphqlSchemaFieldFactory, json/Table, json/Column | **E3 DONE** — SchemaMetadataGraphqlSurfaceTest.graphqlSurfaceExposesInheritNamesInheritIdsAndColumnValues / jsonModelColumnValuesRoundtrip | - |
| JSON/`_schema` `inheritId` carries ALL parents via new `inheritIds` list (scalar `inheritId` kept = parent[0]; no real consumer reads it — frontend recomputes locally) | json/Table, GraphqlSchemaFieldFactory | **E3 DONE** — SchemaMetadataGraphqlSurfaceTest.jsonModelInheritIdsHasBothParentsForDiamondChild | - |
| RDF emits ACTIVE-MODULE COLUMN VALUES (module cols treated like any column) under the host root subject; MODULE tables produce NO standalone RDF (RdfRowsGenerator skips tableType==MODULE); does NOT emit a separate rdf:type per module (Q3 — FHIR meta.profile analog deferred) | Emx2RdfGenerator / RdfGenerator / RdfRowsGenerator | **E4 DONE** — RDFTest.moduleColumnValueEmittedAsTripleWhenModuleActive | - |
| TestMigration asserts migration32→DB v33 upgrade (table_inherits VARCHAR[] + column_metadata."values") | TestMigration (nonparallel module) | **E5 DONE** — TestMigration.testMigration32 (reconstructs pre-v33 scalar state → migrate → asserts ARRAY/_varchar; non-tautological) | - |
| Loadable feature-showcase demo (`diamond_showcase`) exercises diamond + MODULE/MODULE_ARRAY (2 axes) + module-extends-module + ENUM/values + is-a∥composition; loads + queries module cols end-to-end | data/diamond_showcase + DiamondShowcaseLoader + DataModels | **E8 DONE** — DiamondShowcaseTest (9 tests) | Phase F visual |
| Single-file cross-module DISPLAY COLUMN ORDER controllable from one model file via ROW ORDER (Phase H prereq). Module cols report shared root via getRootTableName → export sort interleaves them with root cols; `fromRowList` assigns global positions by file row order BY DESIGN (Emx2.java:151-152 overwrites explicit position — row order is authoritative). NO production change needed | Emx2.toRowList sort / Column.getRootTableName / Emx2.fromRowList | **E6 DONE (verified, lock test)** — Emx2CrossModuleColumnOrderTest.interleavedColumnOrderPreservedAcrossModuleBoundary / explicitPositionInterleavedColumnOrderPreservedAcrossModuleBoundary | - |
| Old extends write path stays automatic (type from target table) | SqlTable | _todo_ | visual check |
| **Phase F (2026-06-14, staged F0–F6; tailwind-first; ONE PR)** — see plan "Phase F REVISED" | | | |
| F0: module CONTENT columns are WRITABLE via GraphQL mutation when their module is active (input type widened to module-inclusive set; engine C3 routes, C6 hard-deletes on deselect). Two-site fix: (1) `GraphqlTableFieldFactory` rowInputType loop + getMutationDefinition guard swapped to `getColumnsIncludingSubclassesAndModulesExcludingHeadings()`; (2) `TypeUtils.convertToRows` non-pk path swapped to `getColumnsIncludingSubclassesAndModules()` so module cols pass through to the Row (additive — key-presence guarded at :527, is-a writes unaffected); (3) review follow-up: `convertMapSelection` columnIdentifierMap (:776) widened so module cols resolve directly in query projection (was relying on an implicit string-name fallback) | GraphqlTableFieldFactory.rowInputType (:1019) / getMutationDefinition (:925) / convertMapSelection (:776) + TypeUtils.convertToRows | **F0 DONE + reviewed (2026-06-14)** — TestModuleGraphqlDataMutation: insertWithModuleColumnSucceedsAndIsQueryable (w/ pre-condition assert) / updateModuleColumnValueRoundTrips / deselectingModuleCausesModuleColumnToReadNull; RED="field name 'modCol' not defined for input object type 'MutRootInput'"; GREEN=3/3 + TestModuleGraphqlDataQuery 2/2 + TestTypeUtils 17/17 (incl. convertToRowsModuleColPresentKeyIsWrittenAbsentKeyIsSkipped); is-a regression TestDiamondInheritance 12 / TestInherits 2 green | - |
| F1: frontend metadata exposes `values` (col) + `inheritName/Id/Names/Ids` (table) + ENUM/ENUM_ARRAY/MODULE/MODULE_ARRAY in the type union; a module-expansion composable resolves root MODULE_ARRAY `values`→MODULE table→its local cols (its cols minus the root's) = a module group, with `activeModules(row)` (pure fns in apps/metadata-utils/src/moduleColumns.ts; 2-consumer rule: tailwind forms F2 + ui detail F4) | apps/metadata-utils/src/types.ts (+`values`, inherit lists, ENUM/ENUM_ARRAY/MODULE/MODULE_ARRAY union) + moduleColumns.ts + index.ts, apps/tailwind-components/app/gql/metadata.js, fieldHelpers.ts | **F1 DONE + reviewed (2026-06-14)** — vitest moduleColumns.test.ts (22: no-module/single/two-axis/module-extends-module chain/unknown-name-skip/activeModules none-one-both) + fieldHelpers.test.ts (8: isArrayType/isRefType for ENUM/ENUM_ARRAY/MODULE/MODULE_ARRAY); metadata-utils exit 0 | - |
| F1: ENUM renders as a select over `values`; ENUM_ARRAY as a multi-select (display + input) | apps/tailwind-components value/EMX2.vue (ENUM→ValueString, ENUM_ARRAY→ValueList) + Input.vue (`values` prop) + input/Enum.vue (wraps InputListbox, a11y describedBy→toggle button) + input/EnumArray.vue (wraps InputCheckboxGroup) + Enum.story.vue | **F1 DONE + reviewed (2026-06-14)** — story + vitest; tailwind-components test-ci 169 pass exit 0; format+lint clean. **BUGFIX (2026-06-14, human found in live Add-record form):** the `values` prop was NOT threaded through the form chain (FormField→Field→Input), so ENUM/ENUM_ARRAY arms got `values=undefined` and rendered nothing — the F1 story tested Input in isolation so it slipped review. Fixed: Field.vue declares `values` + passes `:values` to Input; FormField.vue passes `:values="column.values"`. Red-green test FormField.spec.ts ("Field.vue values threading", 4 cases) renders the FORM path and asserts the widget appears (RED: `[role=listbox]` absent → GREEN) | theme×size matrix (deferred to F6) |
| F2: a MODULE_ARRAY axis renders as a multi-select of module names; selecting a module reveals a collapsible labeled fieldset of that module's columns; deselect hides+clears (→ C6 delete); writes discriminator + active module cols to root mutation | tailwind-components useForm.ts + form/Fields.vue/FormField.vue + Input.vue | _F2 IN PROGRESS_ — **picker slice DONE (2026-06-14):** Input.vue MODULE_ARRAY arm → multi-select over `values` (reuse InputEnumArray) + MODULE → InputEnum; lets a user select modules → discriminator written by existing useForm submit (F0 write verified live). STILL TODO: collapsible per-module content-column fieldset (expandModuleColumns wiring) + show/hide + required-gating (C5 mirror) + deselect-clears; vitest on group/active/required-gating logic + story; e2e F6 | picker reveals/hides groups; write roundtrips |
| F2: a module column's effective `visible`/`required` is gated by module-active — never required when its module is inactive (mirrors engine C5) | tailwind-components useForm.ts visibilityMap/requiredMap | _F2 todo_ — vitest (active+omit→required, inactive+omit→ok) | - |
| F3: data explorer displays module cols (null for non-activating rows, C4) + a MODULE_ARRAY chip cell; filter + order-BY on MODULE_ARRAY/module cols is GATED (backend unsupported, C4-deferred) | tailwind: apps/ui table views + tailwind-components table/CellEMX2.vue; bootstrap safe-degrade: molgenis-components FilterInput/TableExplorer/FilterSidebar/DataDisplayCell | _F3 todo_ — vitest on gating helpers + story; e2e F6 | module col visible; filter/sort hidden for module cols |
| F3: bootstrap (apps/tables via molgenis-components) never CRASHES on a MODULE_ARRAY schema — FilterInput no longer throws, sort guarded, cell renders chips (safe-degrade, no group UX) | molgenis-components FilterInput.vue, TableExplorer.vue, DataDisplayCell.vue | _F3 todo_ | open a MODULE_ARRAY table in apps/tables w/o error |
| F4: record/detail view (apps/ui) renders active-module column groups read-only; MODULE_ARRAY display arm; MODULE tables stay non-top-level in nav (no leak/404) | apps/ui [schema]/[table]/[entity].vue + [schema]/index.vue, value/EMX2.vue | _F4 todo_ — story; e2e F6 | detail shows module groups; MODULE absent from table list |
| F5: schema editor can author a MODULE table (tableType=MODULE), a MODULE_ARRAY column + its `values`, and a multi-parent (diamond) extends; diagrams render diamond edges | apps/schema TableEditModal.vue, columnTypes.js, ColumnEditModal.vue, utils.ts, SchemaDiagram.vue/NomnomDiagram.vue | _F5 todo_ — vitest on utils.ts inherit-list derivation + diagram edge gen | author MODULE/MODULE_ARRAY/diamond end-to-end |
| F6: end-to-end visual verification on diamond_showcase across 5 themes × 3 sizes | playwright-cli on apps/ui + apps/schema | _F6 todo_ | full matrix |
