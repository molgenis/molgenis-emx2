# Decisions (append-only: WHAT changed + WHY + owner + date)

## 2026-06-25 — master merge: entity detail page cell-detail mechanism
**WHAT:** Merged `origin/master` into `feat/details_view_like_catalogue`. Conflict in `apps/ui/app/pages/[schema]/[table]/[entity].vue` reconciled by KEEPING our PR's `DetailView` smart-mode body and ADOPTING master's `CellDetailModal` + single `cellDetailPayload` ref + `useSession` (edit/delete perms). Our PR's older scattered cell-detail flow (`TableCellDetailRef`, `handleDetailRefClick`, `cellDetailColumn`/`cellDetailSchemaId`/`cellDetailValue`, `isRefLikeDetail`/`isArrayLikeDetail`, `toRefColumn*`, `Modal`) was dropped as dead code.
**WHY:** Master independently rewrote the same page and its `CellDetailModal` is the newer self-contained component (handles array/single-ref/scalar internally, delegating to `TableCellDetailRef`). The auto-merge already wired `CellDetailModal`/`cellDetailPayload` into the common regions, so one consistent mechanism avoids a broken hybrid.
**VERIFIED:** dropped+recreated `molgenis` DB; backend with `MOLGENIS_INCLUDE_CATALOGUE_DEMO=true`; browser-checked catalogue-demo Collections (RAINE) and Datasets (AVOHILMO-avo_toimenpide) detail views — both render, no console errors. nuxi typecheck exit 0; prettier clean.
**ALSO:** `data/_models/shared/Resources.csv` conflict resolved = keep HEAD (adds `display`/`role`/`label` columns + 3 `*heading` rows) + master's 3 IRI→prefix fixes (`fdp-o:metadata*`, PR #6394).
**OWNER:** mswertz

## 2026-06-25 — clean re-merge of latest origin/master (7149fe91b)
**WHAT:** Re-merged `origin/master` 7149fe91b so it is now a TRUE ancestor of HEAD (the prior merge commit `e61652121` only recorded an old master, `ca65d2354`, and origin/master had advanced). Sequence: checkpoint commit `1099636a1` (the previously-staged detail-view + resource-split tree) → `git merge origin/master` → merge commit `6e5496555` (parents = checkpoint + 7149fe91b). Zero conflicts. Genuinely-new master commits absorbed: #6402 (remove non-layout code from default layout: `AccountMenu.vue` + `useLayout{Head,Menu,State}.ts` + `default.vue`), #6410 (graphql null file-scalar in inherited tables: `SqlQuery.java` + `TestTableQueriesWithInheritance`), #6409 (single modal overflow). Also prettier-formatted `DetailColumn.vue` (was the only CI format blocker).
**WHY:** Without a real 3-way merge, origin/master was not an ancestor → future merges would re-conflict. A real merge (vs `-s ours`) verifies master's changes are incorporated rather than silently dropped — important because 16 files had been touched by both sides historically.
**VERIFIED:** review confirmed empirically (per-file diff vs origin/master AND vs checkpoint) all master changes incorporated, all branch features intact, no conflict markers, no reverted master files. Frontend: `tailwind-components` 325/325 + `metadata-utils` 2/2 pass, all lint clean, format clean. Backend: `molgenis-emx2` 70/0, `molgenis-emx2-graphql` 55/0, `molgenis-emx2-io` 80/0 PASS. `molgenis-emx2-sql` (2 deterministic permission tests + 1 flaky JWT) and `molgenis-emx2-webapi` (1 env-only script-scheduling) FAIL but PROVEN pre-existing — the 3 SQL failures reproduce on pristine master `0bb17af4`, the failing test files are byte-identical to master, and the owning permission commits predate the merge-base.
**ALSO:** Permanently removed the `git commit` guard in `.claude/settings.local.json` (kept the `git push` guard) per owner request; lead still commits only when instructed.
**OWNER:** mswertz

## 2026-06-26 — PR #6115 CI: e2e wontfix, gated on preview loading
**WHAT:** PR #6115 CI status: `test` + Azure + build/previews/pypi GREEN (the `JWTgeneratorTest.testInvalidSignatureThrowsException` failure was a known flake — cleared on re-run; confirmed passing on master's Azure). Two `e2e-tests` selector collisions were FIXED + committed (`addModal.spec` commit `40ccbe095`, `editModal.spec` commit `11d683160`): the new `display/DetailSection.story` adds a left-nav link "DisplayDetailSection" whose name contains the substring "details", so `getByRole("link",{name:"details"})`/`filter({hasText:"details"})` matched 2 elements → fixed with `exact:true`. The REMAINING `e2e-tests` failures are PRE-EXISTING tests asserting the OLD data structure / OLD detail rendering, broken by the (approved) resource-table split + detail-view redesign — e.g. `externalRefDetail.spec` expects a `heading` "catalogue type", but `catalogueType` is now an ONTOLOGY field rendered as a definition-term label, not a heading. CI surfaces them one-at-a-time: the `e2e-tests` job runs 5 sequential suites (tailwind-components, directory, ui, non-ssr, catalogue) and aborts at the first failing step (maxFailures=1).
**DECISION:** WONTFIX the remaining e2e tests on this poc PR, **conditioned on the app itself loading correctly on the PR preview** (`preview-catalogue-pr-6115.dev.molgenis.org`). The `e2e-tests` CI check stays red by accepted choice. Updating the stale assertions to match the new model/rendering is a separate effort tracked for later.
**WHY:** Owner prioritizes a working app over green e2e for this poc; the new behavior is correct (verified locally + via preview smoke), so the e2e reds are stale expectations, not regressions.
**OWNER:** mswertz

## 2026-06-30 — REVERSAL: the remaining e2e "stale" failure was a REAL regression → fixed (not wontfixed); re-merged master (ed1e18055)
**WHAT:** Re-investigating the remaining `e2e-tests` red (`externalRefDetail.spec` expecting a "catalogue type" heading) against the PR PREVIEW (a valid env, unlike the broken local one) showed it was NOT a stale assertion but a REAL regression: this branch's `apps/tailwind-components/app/components/value/Object.vue` redesign dropped the click-to-open `CellDetailModal` — it replaced master's `<div @click="handleRefCellClicked">` wrapper with a non-clickable `<span>` + "Read more" tooltip and left `handleRefCellClicked` as dead code. That silently removed cell-detail drill-in for ONTOLOGY + ref-array/multiselect/checkbox value cells in BOTH the table and detail views. Owner decision: UNINTENDED → restore the click. Fix (commit `2bb1c65bc`): wrapped the value spans in a clickable `<span class="underline hover:cursor-pointer text-link" @click="handleRefCellClicked">`, KEEPING the new name/space-separated display + CustomTooltip (tooltip kept outside the click target). `externalRefDetail.spec` now passes AS-IS (CellDetailModal title → `Modal.vue` `<h2 id="modal-title">` = role=heading "catalogue type") — NO test edit needed. `renderForm.spec` needed no change. The two earlier selector-collision fixes stand (addModal `40ccbe095`, editModal `11d683160`).
**WHY (reversal of the 2026-06-26 wontfix):** the 6/26 "e2e is stale, not a regression → wontfix" assessment was WRONG for externalRefDetail — it was based on a broken local env. Verifying on the preview exposed a genuine lost-behavior bug, so we FIX rather than wontfix. Any *truly*-stale e2e still hidden behind CI fail-fast / in the directory/ui/non-ssr/catalogue suites remains TBD on the next CI run; wontfix applies only to genuinely-stale ones.
**ALSO:** Re-merged `origin/master` (now `ed1e18055`; HEAD was 76 ahead / 18 behind) — ZERO conflicts (4 files clean 3-way auto-merge: TableEMX2.vue, Column.java, TableMetadata.java, use_schema.md), ancestry OK, no reverted master content. Merge commit `63ccdb43d`.
**VERIFIED:** Object.vue fix + Modal `<h2>` chain confirmed by lead code-read; NOT live-run (local env broken) — CI is authoritative. Merge sanity: `git diff origin/master --name-only` = branch-domain files only.
**OWNER:** mswertz
