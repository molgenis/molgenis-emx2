categories:
  - MOLGENIS
questions:
  - variable: environment
    label: Environment
    description: "Environment of MOLGENIS instance"
    type: enum
    options:
      - edge
      - dev
      - test
      - accept
      - prod
    required: true
    group: "Provisioning"
