package org.molgenis.emx2.rdf.generators;

/**
 * Root RDF generator is separate as it does not follow the general logic of a config defining its
 * behavior but follows its own logic.
 */
public class RootRdfGenerator {}
